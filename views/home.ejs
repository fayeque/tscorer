<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
    <title>Document</title>
    <style>
    .bottom{
    height: auto;
    display: flex;
    flex-direction: column;
    background-color: lightgray;
    /* height: 100%; */
    }
    .section1{
    margin-top:0.5rem;
    margin-left: 1rem;
    margin-right: 1rem;
    height:15%;
    border-radius: 10px;
    box-shadow: 0 2px 8px rgba(0,0,0.26);
    background-color: white;
    padding: 1rem;
    }
    </style>
</head>
<body>
    <div style='display:flex;height:8vh;padding:5px;background-color:darkgreen;color:white'>
        <h2><a style='color:white' href="/">Tarbangla</a></h2>
        <h4 style='padding-top:8px;margin-left:5px'>Scorer</h4>
    </div>
    <div class='bottom'>
        <% matches.forEach((m) => { %>
            <div class='section1'>
                <h6><%= m.details.date.split('T')[0] %></h6>
                <div style="display:flex;justify-content:space-between;"><div><%= m.details.batting %></div> <div style="display:flex;"> <h4><%= m.details[m.details.batting].runs %>/<%= m.details[m.details.batting].wickets %></h4> <h5>(<%= m.details[m.details.batting].overs %> . <%= m.details[m.details.batting].balls %>)</h5></div></div>
                <div style="display:flex;justify-content:space-between;"><div><%= m.details.bowling %></div> <div style='display:flex;'> <h4><%= m.details[m.details.bowling].runs %>/<%= m.details[m.details.bowling].wickets %></h4> <h5>(<%= m.details[m.details.bowling].overs %>.<%= m.details[m.details.bowling].balls %>)</h5></div></div>

                <div style='display:flex;margin-top:15px;justify-content:space-around'>
                    <% if(!m.details.gameOver){ %>
                        <h6 style='cursor:pointer;'>
                            <a href="/match/<%= m.matchId %>">Live score</a>
                        </h6>
                        <% } %>
                <h6 style='cursor:pointer;'>
                    <a href="/match/scorecard/<%= m.matchId %>">Score card</a>
                </h6>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>